# AI Voice Interviewer - Mercor-Grade Interview System

Профессиональная система AI-интервью с адаптивным пайплайном в стиле Mercor, включающая:

- 🎤 **Real-time голосовое интервью** с Google Gemini Live API
- 🧠 **Адаптивная генерация вопросов** с follow-up логикой
- 📊 **Refine-цепочка оценки** (Draft → Refine) для точных результатов
- 🛡️ **Античит-система** с анализом таймингов и паттернов
- 🎯 **Эвристическая оценка** в реальном времени без задержек
- 🔄 **Поддержка OpenAI и Gemini** через админ-панель

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Настройка переменных окружения

Создайте файл `.env.local`:

```env
GEMINI_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key  # опционально
```

### Запуск

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 🏗️ Архитектура

### Mercor-Grade Pipeline

1. **Real-time эвристическая оценка** - быстрый анализ ответов без LLM на каждом шаге
2. **Адаптивная генерация вопросов** - follow-up при неполных ответах, пропуск тем при отрицательных ответах
3. **Refine-цепочка финальной оценки**:
   - Draft Pass: `models/gemini-1.5-flash-latest` (быстрая модель)
   - Refine Pass: `gpt-4o` (мощная модель для финальной оценки)
4. **Античит-модуль** - анализ таймингов, паттернов ввода, сходства с LLM-ответами

### Компоненты

- `services/interviewPipeline/pipeline.ts` - основной пайплайн интервью
- `services/interviewPipeline/evaluator.ts` - эвристическая и LLM-оценка
- `evaluation/evaluator.ts` - финальная оценка с refine-цепочкой
- `evaluation/antiCheat.ts` - античит-анализ
- `components/InterviewScreen.tsx` - UI интервью с RNNoise шумоподавлением

## 🎛️ Конфигурация моделей

### Поддерживаемые модели

**OpenAI:**
- `gpt-4o`
- `gpt-4o-2024-05-13`
- `gpt-4-turbo-2024-04-09`
- `gpt-4-1106-preview`

**Google Gemini:**
- `models/gemini-2.5-pro` (флагманская)
- `models/gemini-1.5-pro-latest` (стабильная)
- `models/gemini-1.5-flash-latest` (быстрая)
- `models/gemini-2.5-flash-native-audio-preview-09-2025` (для live audio)

### Админ-панель

Доступ к настройкам моделей через админ-панель в интерфейсе приложения.

## 🔧 Технологии

- **React 19** + **TypeScript**
- **Vite** - сборка
- **Google Gemini Live API** - голосовое интервью
- **OpenAI API** - финальная оценка (refine pass)
- **RNNoise** - шумоподавление аудио
- **Web Audio API** - обработка аудио

## 📝 Особенности

### Адаптивная логика вопросов

- **Follow-up вопросы** при неполных ответах (score < 70)
- **Пропуск тем** при отрицательных ответах (нет опыта)
- **Переход к новым темам** при хороших ответах (score >= 70)
- **Отслеживание количества follow-up** по каждой теме

### Эвристическая оценка

- Проверка ключевых слов (70% веса)
- Анализ полноты ответа (длина, примеры, метрики)
- Определение отрицательных ответов
- Обнаружение формальных/шаблонных ответов

### Refine-цепочка

1. **Draft Pass** - быстрая модель создает черновую оценку
2. **Refine Pass** - мощная модель улучшает и финализирует оценку
3. **Fallback** - если refine не удался, используется draft

## 🛡️ Античит-система

- Анализ таймингов (задержки перед ответом)
- Обнаружение паттернов ввода (copy-paste)
- Проверка сходства с LLM-ответами
- Анализ формальности и шаблонности ответов

## 📦 Структура проекта

```
ai-voice-interviewer/
├── components/          # React компоненты
├── services/            # Бизнес-логика
│   └── interviewPipeline/  # Пайплайн интервью
├── evaluation/          # Модули оценки
├── config/             # Конфигурация
├── public/             # Статические файлы (RNNoise)
└── types.ts            # TypeScript типы
```

## 🔐 Безопасность

- API ключи хранятся в `.env.local` (не коммитятся в git)
- Поддержка прокси-сервера для API ключей в продакшене
- `dangerouslyAllowBrowser: true` только для разработки

## 📄 Лицензия

Private project

## 🤝 Вклад

Проект в активной разработке. Pull requests приветствуются!
